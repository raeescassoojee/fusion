# Sentinel Final v7

This folder is a separate v7 build; the supplied v6 archive remains unchanged.

## Verified demo behaviour

- The member heat map is fixed to the three Ness Avenue homes in Lakefield:
  Thandi at 10, Amy at 11, and Fatima at 12 Ness Avenue.
- All three house dots use distinct saved coordinates, with an always-visible
  outline above the basemap so house 11 cannot disappear behind map styling.
- A red line and directional arrows appear only after an active incident has an
  accepted handoff across at least two distinct cameras. There is no permanent
  decorative line between houses.
- A new handoff refits the map automatically so the complete route is visible.
- Security dispatches repair legacy Killarney destinations to the live Ness
  Avenue incident coordinate, and response routes terminate at that exact point.
- Claims retain populated checklist evidence including the police case number,
  show percentage completion (the Cassoojee demo starts at 30%), and move to
  Ready for review after the automated review.
- The incomplete patrol optimisation panel, demo consoles, and WhatsApp message
  log remain removed from the visible security workspace.
