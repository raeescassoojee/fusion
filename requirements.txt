numpy>=2.0,<2.3
opencv-contrib-python-headless==4.12.0.88
pillow>=11,<13
pydantic>=2.10,<3
PyYAML>=6,<7
pytesseract>=0.3.13,<0.4
rapidfuzz>=3.10,<4
boto3>=1.40,<2

# --- API server (were previously undeclared) ---
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1

pandas>=2.2,<3            # claims workbook for fraud analytics
openpyxl>=3.1,<4          # xlsx reader
scikit-learn>=1.5,<2      # reproducible 80/20 claims and hotspot training pipelines
joblib>=1.4,<2            # persisted model artifacts

# --- camera intake + evidence patterns ---
python-multipart>=0.0.9   # file upload for /api/cameras/upload
